Import('env')
env.Append(LIBS=Split('-lglfw -lGL -lGLEW'))

# TODO(clchiou): This is problematic; figure out how to do this in scons.
archives = ['#/out/graphics/libgraphics.a',
            '#/out/platform/graphics/linux/libplatform_graphics.a']

env.Program(target='pong', source=Glob('*.cc') + archives)
